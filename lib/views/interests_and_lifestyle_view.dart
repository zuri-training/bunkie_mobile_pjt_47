import 'package:flutter/material.dart';

class InterestsAndLifeStyleView extends StatelessWidget {
  const InterestsAndLifeStyleView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
